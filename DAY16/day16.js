/**
 * Query selector
 * querySelector is a method of Document and it returns the first element that matches the specified selector, or group of selectors.
 * If no matches are found, null is returned.
 * Synthax:
 * querySelector(selectors)
 *
 *
 */

/**
 * addEventlistener
 * The addEventListener method of the EventTarget interface sets up a function that will be called whenever the specified event is delivered to the target.
 * Common targets are Element (or its children), Document, and Window.
 * But the target may be any object that supports events
 * Synthax:
 * addEventListener(type, listener)
 * addEventListener(type, listener, options)
 * addEventListener(type, listener, useCapture)
 */
